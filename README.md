# 🏆 MakeX Inspire 成绩统计系统

面向 MakeX Inspire 队伍训练的**本地化成绩管理与统计工具**（纯静态网页，GitHub Pages 部署）。

所有数据默认保存在本地浏览器（localStorage / IndexedDB），可通过「后台 → 数据迁移」导出 / 导入备份或跨设备同步。

## ✨ 主要功能

- **集训管理**：创建集训、录入模拟赛 / 正赛记录、最佳成绩概览与排名
- **任务管理**：配置任务满分与「目标档位」（满分前提用时目标），支持全局 / 个体目标设定
- **个人成绩卡**：按 模拟练习 / 自主训练 / 整合 查看个人成绩与每项当前目标
- **自主训练**：学生独立录入练习成绩（得分默认满分、自动记录时间），可导出 CSV
- **成绩统计与教务**：统计页、班级 / 学员管理、数据迁移与开发者工具

## 🗂 页面导航

| 分类 | 页面 |
|------|------|
| 🏠 首页 | 工具中心（统一入口） |
| 🏆 赛事 | 成绩统计 / 任务管理 / 成绩展示 |
| 📚 后台 | 教务管理 / 个人成绩卡 / 数据迁移 / 开发者工具 |
| 其他 | 自主训练 / about（含修订记录） |

## 📝 修订记录

### 2026-08-28
- ⚙️ **优化** 教练评语：点击框内编辑 + 失焦自动保存 — 去掉「✏️ 编辑」按钮：左栏「教练原始评语」保留外框，点击框内任意处直接进入编辑（默认进入编辑，保存后展示有序列表，再点框内即重新编辑）；右栏「最终评语」去掉「💾 保存」按钮，改为失去焦点自动保存；切换视图时左右两栏都会自动收尾保存
- ⚙️ **优化** 教练补充：左右双栏 + 复制AI数据包 + 数据结构说明 — 教练补充区新增「🔗 复制AI数据包」按钮（无需打开 AI 弹窗，一键复制含提示词/数据说明/评估数据/现有教练补充的完整数据包）；区域改为左右双栏：左栏为教练补充内容（编辑/AI生成），右栏为「📥 粘贴 AI 最终版」输入框 + 导入按钮，可直接把 AI 生成的最终版粘贴进来写入教练补充；复制数据包时自动附带教练现有输入（【现有教练补充】）；「🧩 编辑元素」新增「AI 数据说明」字段（可配置，默认描述各数据区块字段含义：学员信息/参赛记录/任务统计/量化综合/现有教练补充），随数据包一起发送/复制，帮助 AI 理解评估数据结构
- ⚙️ **优化** 评估报告：填写模式隐藏只读区块（含各任务表现趋势） — 「✍️ 填写」模式下隐藏所有只读区块：「🏆 参赛记录」、「📊 量化评估（综合）」、「📈 各任务表现趋势」（老师均无需操作），仅保留需要填写的部分（量化评估表打分、教练补充、头部信息）；切到「👁 导出预览」或导出 PDF 时全部正常显示；切换预览时雷达图与任务折线图均以可见尺寸重绘
- ⚙️ **优化** 评估报告：页面工具条独立且始终可见 — 标题 + 视图切换（✍️填写/👁导出预览）+ 编辑元素/生成评估/导出PDF + 学员班级筛选，从报告卡片中抽出为报告上方独立的「页面工具条」（浅灰底、边框、与白色文档卡片区分），按页面工具布局呈现；在填写与导出预览两个视图下这些页面工具均始终可见，预览视图仅隐藏文档内部编辑控件（AI/编辑/重置按钮、评分输入框转文本只读）；导出 PDF 时页面工具条不打印
- 🆕 **新增** 评估报告：填写 / 导出预览 双视图（同窗口切换） — 报告卡头部新增「✍️ 填写 / 👁 导出预览」视图切换：填写视图为教练信息采集界面（评分、评语、AI 生成、编辑元素等全部可编辑）；导出预览为同窗口的干净预览，隐藏全部编辑控件（工具栏、生成/编辑元素/AI/重置按钮、评分输入框转纯文本并只读），教练补充按「教练评语」正式标题 + 引文式排版呈现，与导出 PDF 效果一致；「🖨 导出 PDF」按钮在预览视图仍可用
- ⚙️ **优化** 教练补充：导出 PDF 时标题改为「教练评语」+ 引文式排版 — 前台数据采集界面（含 ✏️ 编辑 / ✨ AI 生成）仍叫「教练补充」；导出 PDF 时该部分标题自动改为「💬 教练评语」（data-print-title 打印切换），评语正文采用引文式排版（蓝色左边框 + 内边距），与数据采集语境区分、更符合呈给家长的正式报告
- 🆕 **新增** AI 生成：可编辑提示词（嵌入数据包） — 「🧩 编辑元素」弹窗新增「教练评语 AI 要求提示词」字段（全局设置，默认：资深少儿机器人老师、写一份180字左右评语、一段话描述、积极正面且客观公正），可自由修改；提示词会以【评估数据】形式合理嵌入 AI 数据包——直接调用 AI 或「复制数据包」粘贴到任意 AI 时都携带该要求；AI 弹窗生成按钮文案改为「🚀 调用 AI 生成」（跟随提示词要求，支持单篇或多版本【版本N】解析）
- ⚙️ **优化** AI 生成：一键服务商预设 + 免 Key 方案 — AI 弹窗配置区新增服务商一键选用（OpenAI / DeepSeek / Moonshot-Kimi / Ollama 本地免 Key），自动填入接口地址与模型，只需粘贴一次密钥（本地保存）；API 密钥改为可选（Ollama 本地或无需鉴权的接口可留空，留空则不发送 Authorization 头）；配置区补充使用说明：密钥仅存本机、不想配 Key 可用 Ollama 本地或复制数据包到任意 AI 生成后粘贴回解析
- 🆕 **新增** 评估报告：AI 生成教练补充 — 「教练评语」更名为「教练补充」；其右侧新增「✨ AI 生成」按钮，弹出弹窗：自动打包学员评估数据（学员信息 / 参赛记录 / 任务表现统计 / 量化评估综合），可直接调用 OpenAI 兼容接口（弹窗内配置 接口地址/密钥/模型，本地保存）生成 3 个版本评语，一键「选用」写入；也可「复制数据包」到任意 AI 工具，或手动粘贴 AI 回复按【版本N】/编号解析导入
- 🆕 **新增** 评估报告：报告元素编辑器（标题 + 学员信息行） — 报告卡头部右上角新增「🧩 编辑元素」按钮，弹出编辑弹窗配置报告页面元素，首个元素为「页面标题」；报告顶部改为居中大标题 + 学员信息行（小字）：学员（自动带出，可改）、班级（自动带出，可改）、上课时间、教练、报告填写日期（默认当天，均按学员本地保存，缺省字段可补充填写，填写后显示、未填不显示）；原模块一「学员总览」卡片标题移除，由居中的页面标题替代
- 🆕 **新增** 评估报告：一键导出 PDF + 间距优化 — 报告卡头部新增「🖨 导出 PDF」按钮，调用浏览器打印（可另存为 PDF）；新增 @media print 打印样式：隐藏站点头部/侧边栏/底部/工具栏与按钮、解除滚动容器裁剪避免内容被截断、量化评分输入框打印时转为纯文本、表格/雷达/区块不跨页断行、A4 页面；同时压缩报告嵌套卡片的双重边距与内边距（外层卡横向内边距 24px→12px、内层卡去阴影改细边框、卡片间距由 1rem 双层叠加收敛为 0.75rem 单层），量化评估（综合）可用横向宽度由约 698px 提升至约 746px，雷达图随之放大；生成报告后自动滚动到报告区
- ⚙️ **优化** 评估报告：量化评估表子维度单元格合并 — 量化评估表中同一子维度下有多条评价细则时，子维度名称单元格纵向合并（rowspan），每个子维度名称只出现一次，减少重复、表格更紧凑；表头「小维度」统一改称「子维度」
- ⚙️ **优化** 评估报告：A4 版式 + 雷达图恒居汇总表右侧 2×2 + 即时刷新 — 评估报告卡片按 A4 宽度（794px）排版，适合直接转 PDF 呈现给家长；「量化评估（综合）」改为评分汇总表在左、四个雷达图恒居其右侧固定 2×2 排布（同一行，不随窄屏折叠），画布尺寸随可用宽度自动适配、标签不裁切；量化评估表打分后，评分汇总表与雷达图即时刷新（无需刷新页面），底部量化评估表合计同步更新
- 🆕 **新增** 集训新增「赛事名称」属性 — 新建/编辑集训弹窗中新增「赛事名称」可选字段（如 MakeX Inspire 2026 全国总决赛），保存至集训数据 competitionName 字段，用于标识该集训对应的赛事全称；评估报告「参赛记录」表的赛事列优先显示该赛事名称，未填写时回退到记录名称

### 2026-08-27
- 🆕 **新增** 后台新增「评估管理」入口 — 新增评估管理页（训练评估报告）：针对单个学员评估，学员采用 班级→学员 两级导航；评估范围在独立弹窗中多选（集训 + 赛事/数据集，默认全选，支持全选/清空）；报告最顶部为「参赛记录」表（扁平三列表格：赛事 | 基础任务得分（如 800分/40s） | 成绩，未参加填「未参加」；仅正赛，不含集训内部模拟赛记录；正赛成绩录入新增「奖项」列可手动填写奖项如 一等奖/冠军，报告成绩列优先显示手动奖项，未录入时按名次回退）；下方为 评估结论（评级/建议）+ 概览统计 + 各任务表现折线图（每任务一张图，得分与用时同图双线对比，不显示具体数值，无数据的任务不展示）；底部新增「量化评估表」（知识与技能 / 赛事能力 / 个人能力 三维度，维度→小维度→评价细则，可打分并自动合计，按学员本地保存）；参赛记录表与折线图之间新增「量化评估（综合）」：评分汇总表（维度/子维度 评分、参考值、同龄指数=评分/参考值）+ 四个雷达图（三维度 + 综合评分，评分蓝/参考灰），参考值在模板中可配置；其下新增「教练评语」卡片（✏️ 编辑按钮一键编辑，多行文本按学员本地保存，刷新后回显）；报告按三大模块组织：模块一「学员总览」（姓名 + 参赛记录 + 量化评估（综合）+ 教练评语，单卡片内分节）、模块二「各任务表现趋势」（折线图 + 数据统计）、模块三「量化评估表」（可打分细表）；入口位于后台侧边栏
- ⚙️ **优化** 个人成绩卡：取消自主训练录入入口，改为标题右侧导入按钮 — 移除个人成绩卡内的自主训练录入表单；在「个人成绩卡」标题右侧新增「📥 导入自主训练」按钮（位于标题与学员选择之间），自主训练记录统一通过导入 CSV 录入
- ⚙️ **优化** 个人成绩卡：练习记录改为训练成绩记录表 — 「我的练习记录」改为「训练成绩记录表」：汇总该学员全部集训的赛项记录（模拟赛/正赛）与自主训练记录；支持按集训、赛项、任务筛选，可导出当前筛选结果

### 2026-08-11
- 🆕 **新增** 新增 about 独立入口 — header 右侧新增 about 独立入口：正文为工具说明；原放置在后台的修订记录（changelog）移入 about 显示
- ⚙️ **优化** 统一 header 按钮显示样式 — 分类 tab / 自主训练 / about 采用一致三态（普通 / 悬停 / 激活）
- 🆕 **新增** 新增目标设定 — 集训新增「目标设定」弹窗（全局 / 个体页签），可配置目标档位；个体默认沿用全局档位；已结束集训（含正赛）不显示目标
- ⚙️ **优化** 集训成绩显示：用时 / 得分位置对调 — 集训页成绩统一改为「用时在前、得分在后」；覆盖最佳成绩概览、综合最佳、记录行与统计行

### 2026-08-10
- 🆕 **新增** 任务满分用时目标（目标档位 + 当前目标） — 任务可配置目标时间档位；集训概览与个人成绩卡显示「当前目标」与达标徽章；支持 模拟练习 / 自主训练 / 整合 切换

### 2026-08-07
- 🆕 **新增** 学生独立成绩录入页（stu_tool） — 首次进入需设定姓名；数据本设备独立保存，可导出 CSV
- 🆕 **新增** 成绩录入：得分默认满分 + 自动记录时间 — 支持任务类型 + 得分 + 用时快捷录入
- 🆕 **新增** 失误点标注（点选多选 + 自定义） — 常用失误类型点选 + 手动输入
- 🆕 **新增** 成绩分析（满分区间 / 满分率 / 失误率） — 基础 / 随机分开统计
- 🆕 **新增** 顶部导航新增「成绩录入」学员入口
- 🆕 **新增** 个人成绩卡：来源切换 + 自主训练录入 — 支持导出 / 导入
- ⚙️ **优化** 成绩录入卡片高亮 — 暖橙高亮背景与边框，方便学生快速定位

### 2026-08-01
- ⚙️ **优化** 主站「赛事」改为登录后显示
- 🆕 **新增** 修订记录按日期分组折叠
- 🔧 **修复** 集训成绩录入后主内容不自动刷新
- ⚙️ **优化** 拆分新增班级 / 新增学员卡片
- 🆕 **新增** 学员视图切换（按班级 / 按学员）
- 🔄 **重构** 班级管理 + 学员管理合并为教务管理
- 🆕 **新增** 数据迁移页 — 数据备份 / 跨设备同步统一入口
- 🆕 **新增** 开发者工具页 — 存储引擎切换 + 数据库管理独立成页
- ⚙️ **优化** 集训详情「编辑学员」更名「设置」
- ⚙️ **优化** 主站右上角改为「开放工具」
- ⚙️ **优化** 移除 header 开发提示与统计条
